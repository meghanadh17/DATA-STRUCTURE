#include<stdio.h>
fibo(int n){
	if(n<=1)
	return n;
	return fibo(n-1)+fibo(n-2);
}
int main(){
	int n;
	printf("Enter the no of terms: ");
	scanf("%d",&n);
	printf("Fibonacci series: ");
	for(int i=0;i<n;i++){
		printf("%d,",fibo(i));
	}

	return 0;
}
